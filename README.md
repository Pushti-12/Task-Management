# Task-Management
A Node.js/Express.js backend API that supports: User Management, User registration and User login, returning a JWT. Authenticated endpoint to retrieve user profile. Task Management by Authenticated users. Tasks should be associated with the user who created them. Database Integration: Store user data in PostgreSQL. and Store task data in MongoDB.
